services:
  frontend:
    build:
      context: ./frontend/vueFrontEndService
      dockerfile: Dockerfile
    container_name: vue-frontend
    ports:
      - "8080:80"
    environment:
      - VITE_API=${VITE_API}
      - VITE_WS=${VITE_WS}
    restart: unless-stopped